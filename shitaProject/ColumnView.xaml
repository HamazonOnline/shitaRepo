<UserControl x:Class="shitaProject.ColumnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:shitaProject"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="150">
    <UserControl.Resources>
        <Style TargetType="Grid">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Path=Locked}" Value="1">
                    <Setter Property="Background" Value="Pink" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Path=Locked}" Value="0">
                    <Setter Property="Background" Value="Yellow" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>>
        <Border BorderBrush="Black" BorderThickness="5">
        <Grid >
            <Label Name="DoorNameLbl" HorizontalAlignment="Center" VerticalAlignment="Center" Content="1" FontSize="">
                
            </Label>
        </Grid>
    </Border>
    
</UserControl>
